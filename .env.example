# === Embeddings provider ===
# Si defines OPENAI_API_KEY, usará text-embedding-3-large.
# Si no, usará modelo local (all-MiniLM-L6-v2).
OPENAI_API_KEY=
EMBEDDING_MODEL=text-embedding-3-large

# === Chroma ===
CHROMA_DIR=./chroma

# === Auth (token para /ingest) ===
API_BEARER_TOKEN=pon_aqui_un_token_largo_seguro

# === Ingest ===
API_URL=http://localhost:8000/ingest
INGEST_FOLDER=./data
